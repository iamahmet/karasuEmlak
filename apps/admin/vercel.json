{
  "buildCommand": "cd ../.. && NODE_ENV=development pnpm install && pnpm run build:admin",
  "devCommand": "cd ../.. && pnpm run dev:admin",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "git": {
    "deploymentEnabled": {
      "main": true
    }
  },
  "crons": [
    {
      "path": "/api/content/publish-scheduled",
      "schedule": "*/5 * * * *"
    }
  ]
}
